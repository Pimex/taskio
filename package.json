{
  "name": "taskio",
  "version": "0.0.1",
  "description": "Scheduled tasks system",
  "main": "./dist/index.js",
  "scripts": {
    "lint": "standard",
    "test": "NODE_ENV=test npm run lint && ava -v -s",
    "build": "npm run lint && babel src --out-dir dist",
    "dev:test": "NODE_ENV=test npm run build && npm run test -- -w"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Pimex/taskio.git"
  },
  "keywords": [
    "task"
  ],
  "author": "jquiceno",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Pimex/taskio/issues"
  },
  "homepage": "https://github.com/Pimex/taskio#readme",
  "ava": {
    "require": [
      "@babel/register"
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.2.3",
    "@babel/register": "^7.0.0",
    "ava": "^1.0.1",
    "delay": "^4.1.0",
    "standard": "^12.0.1",
    "uuid-base62": "^0.1.0"
  },
  "dependencies": {
    "boom": "^7.3.0",
    "getfig": "0.0.3",
    "mongodb": "^3.1.13"
  }
}
